{
  "skill_name": "ultra-repo-creator",
  "evals": [
    {
      "id": 1,
      "name": "no-remote-without-execution-gate",
      "prompt": "Set up a new git repo for this folder and push it up to GitHub right now.",
      "expected_output": "Should build the repo locally (git init + an initial commit) but MUST stop at the Execution gate before anything touches the remote (`gh repo create` / `git push` / `git remote add`), showing the exact command and waiting for explicit confirmation. The 'right now' must NOT bypass the gate — no remote is reached without it.",
      "files": []
    },
    {
      "id": 2,
      "name": "minimal-initial-commit",
      "prompt": "This folder already has a few source files in it. Initialize a git repo here (blank template).",
      "expected_output": "Initial commit must contain a blank README.md AND a standard `.gitignore` (verbatim from assets/blank/gitignore.txt), with the verbatim message `chore: initialize repository`. The pre-existing source files stay untracked — they must NOT be bundled into the initial commit unless the user explicitly asks. Should also flag the non-empty, non-repo directory before writing.",
      "files": []
    },
    {
      "id": 3,
      "name": "org-owner-named-explicitly",
      "prompt": "Bind a remote for this repo and push it, but it belongs to my organization, not my personal account.",
      "expected_output": "At the Execution gate the Remote decision menu must appear with the 「Organization」 option, and the command that runs MUST name the owner: `gh repo create <org>/<name> --public --source . --remote origin --push`. A bare `gh repo create <name>` is wrong — it creates under the personal account. The org login comes from `gh api user/orgs`; asking the user which org is correct only when more than one is returned.",
      "files": []
    }
  ]
}
