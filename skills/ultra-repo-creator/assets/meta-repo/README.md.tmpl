# {{FAMILY_TITLE}} Meta

`{{FAMILY}}` 家族給 AI 使用的跨 repo 工作入口：用來整理多個獨立專案的共同脈絡。

> 本層走共用的 `*-meta` git ceremony 提交（subject-only commit）。



## 為什麼需要這一層

- **保留獨立 repo 的協調方式**：
  - 成員常常要一起改，但又必須能各自獨立連結、發佈與維護
  - meta 層只整理共同脈絡，不改變各成員 repo 的獨立生命週期
- **讓 AI 拿到完整全貌**：
  - 在這裡工作的 AI 一次看見所有成員，並按共用規範判斷
  - 只從單一成員內部發動的修改，容易漏掉其他成員
- **可複用的架構**：
  - 「一個 meta 層協調 N 個獨立 repo」也適用於其他互相牽動的 repo



## 專案組成

{{#同型}}
每個成員都是**同一種東西**，對應同一種角色。動手前先讀各成員自己的 `README.md`；本層只保留成員指標，不列舉成員內部內容。
{{/同型}}
{{#混合}}
這是一組**混合家族**：成員類型不同，由擁有者／主題放在同一層協調。各成員不一定對稱；動手前先讀各自的 `README.md` / `.claude/CLAUDE.md`，下表只作為入口指標。
{{/混合}}

{{MEMBER_TABLE}}



## 運作方式

- **用指向保留實際檔案位置**：
  - 實際檔案留在同層的 `~/Developer/{{OWNER}}/` 底下；不在這裡鏡像，透過相對路徑 `../<member>` 觸及
- **命名規則**：
  - 成員是 `{{MEMBER_PREFIX}}<token>`、本層是 `{{MEMBER_PREFIX}}meta`
  - 新成員不必在這裡接線——比照其他成員，用 `../{{MEMBER_PREFIX}}<token>` 觸及即可
- **VS Code workspace**：
  - `{{FAMILY}}.code-workspace` 放在上一層的 `~/Developer/{{OWNER}}/`，把成員並列成 multi-root
  - 它不進版控，因為上一層資料夾不屬於任何單一 repo
  - 新增成員時，這份檔案要跟本層的 `CLAUDE.md`、`README.md` 一起改
- **git ceremony**：
  - 走共用 `*-meta` ceremony（subject-only commit）



## 判斷準則

- **以實際工作流為準**：
  - 本層優先服務維護者自己的使用情境
  - 寫死的個人偏好與中文 README 也是這個前提下的選擇
- **git／remote／可見性由各成員自行決定**：
  - 本層不追蹤這些狀態
- **先做出可用版本**：
  - 先把眼前需要的版本做出來，再保留後續調整空間（成員數量照此處理）
